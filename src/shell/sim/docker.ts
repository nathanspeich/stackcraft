// Simulation module: docker. Filled in during phase 6. See src/shell/sim/index.ts.
import type { CommandTable } from './index'

export function install(_base: CommandTable) {
  /* commands are added in phase 6 */
}
